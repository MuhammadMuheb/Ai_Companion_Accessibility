# Core dependencies (text chat + memory + scheduler) — Python 3.10–3.13
requests>=2.31
pyyaml>=6.0
python-dotenv>=1.0
apscheduler>=3.10,<4
adhanpy>=1.0
tzdata

# Browser interface (python main.py --mode web)
fastapi>=0.110
uvicorn>=0.29
python-multipart>=0.0.9

# Optional feature groups are listed in requirements-FULL.txt
