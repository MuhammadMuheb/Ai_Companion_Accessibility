# COMPLETE DEPENDENCIES FOR LOCAL AI COMPANION (Python 3.10–3.13)
-r requirements.txt

# Voice I/O
soundcard>=0.4           # microphone that follows AirPods/earbuds switching; call recording (loopback)
onnxruntime>=1.18        # voice print (speaker verification)
kaldi-native-fbank>=1.20
faster-whisper>=1.1
pyttsx3>=2.98             # Windows built-in voices (Zira, David)
piper-tts>=1.3           # neural voices (Amy, Kristin, Jenny, Ryan …) — offline, downloaded on demand
sounddevice>=0.5
soundfile>=0.12
numpy

# Screen & Vision (also install Tesseract OCR for Windows separately)
pytesseract>=0.3.10
pillow
pyautogui>=0.9.54
pygetwindow>=0.0.9

# System Control
psutil>=6.0
pyperclip>=1.9
pynput>=1.7.7            # global hotkeys (Ctrl+Alt+T translate, Ctrl+Alt+Space talk)
uiautomation>=2.0        # read browser tabs for notifications, drive WhatsApp Desktop

# Windows built-in OCR (screen reading, translate text under the mouse)
winrt-Windows.Media.Ocr
winrt-Windows.Graphics.Imaging
winrt-Windows.Storage.Streams
winrt-Windows.Globalization
winrt-Windows.Foundation
winrt-Windows.Foundation.Collections

# Native app: window (WebView2 — no browser, no server), tray, notifications
pywebview>=5.0
pystray>=0.19
plyer>=2.1
anthropic>=1.0           # optional: Claude for expert mode (your own API key)

# Development
pytest>=8
httpx                    # FastAPI test client
